require_relative '../lib/computer'

describe Computer do
    # context "when we call the #move function" do
    #     it "updates the grid" do
    #         computer = Computer.new
    #         board = double("Board")
    #         allow(board).to receive(:grid).and_return(["O", "X", "X", "X", "O", "O", "X", "O", 9])
    #         allow(board).to receive(:update_grid).and_return(["O", "X", "X", "X", "O", "O", "X", "O", "X"])
    #         computer.move(8, board)

    #         expect(board.grid).to eq(["O", "X", "X", "X", "O", "O", "X", "O", "X"])
    #     end
    # end
end